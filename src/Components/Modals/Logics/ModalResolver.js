import { SETTING } from "../../ConstantStorage";

const ModalResovler = shooter => {

    switch(shooter) {
        case SETTING:

            break;
        default:

    }
    return (
        {/* What should I do? */}
    );
};

export default ModalResovler;